{
  "_comment": [
    "Flux v8 spec for the LIVE Quip faucet, which serves the AGLAIS chain.",
    "",
    "Starting point for the dashboard wizard ONLY — the canonical spec is always the export.",
    "",
    "🔴🔴 CORRECTED 2026-09-07 (QUI-1225). THIS FILE USED TO DESCRIBE A SECOND, PARALLEL APP",
    "CALLED `quipfaucetaglais`. That plan was superseded and the app was NEVER REGISTERED; the",
    "existing `quipfaucet` subscription was repointed at aglais instead. Following the old version",
    "of this file would have registered a SECOND faucet on the SAME funder mnemonic — two",
    "processes, one base wallet, two in-memory nonce lanes — which is exactly the QUI-723/831",
    "stranded-future-nonce failure, aimed at the live faucet. Hence `instances: 1` below is",
    "correctness, not cost, and hence there must be exactly one of these apps.",
    "",
    "Evidence, all from the public Flux API and DNS on 2026-09-07:",
    "  /apps/location/quipfaucetaglais  -> data: []                (never deployed)",
    "  /apps/location/quipfaucet        -> 1 instance, running since 2026-08-28",
    "  dig faucet.aglais.quip.network   -> CNAME quipfaucet.app.runonflux.io",
    "  dig faucet.testnet.quip.network  -> NXDOMAIN                (old chain's name is gone)",
    "  /apps/appspecifications/quipfaucet -> geolocation [acEU, acNA, a!cEU_RU], i.e. the aglais",
    "      geolocation, not the old testnet's [a!cEU_RU]; expire 114846 at height 2917711,",
    "      so the subscription runs to roughly 2026-10-13.",
    "⚠ The live spec itself cannot be read back — Enterprise apps return `compose: []` to the",
    "public API. Confirm against the dashboard export before submitting anything.",
    "",
    "UNCHANGED and deliberate: ports/containerPorts 8087, containerData /data, cpu 1 /",
    "ram 2048 / hdd 1, instances 1, staticip false, repoauth \"\" (the registry is public),",
    "geolocation [acEU, acNA, a!cEU_RU], and the funder key (op-1 = chain sudo).",
    "",
    "QUI-1225 — TELEMETRY. The faucet now ships its own logs to observe.quip.network over OTLP,",
    "so the record of every mint outlives the Flux console (which is wiped on every update, not",
    "only on relocation, and cannot be backfilled). The endpoint and service name are BAKED INTO",
    "THE IMAGE — see the Dockerfile for why — so the only new variable here is the secret,",
    "taking this component from 2 to 3 of the 20 allowed. It is safe in this spec only because",
    "the app is Enterprise; on a non-Enterprise app the Flux public API serves it in the clear.",
    "⚠ Telemetry is off unless the key is present, so omitting it degrades to today's behaviour",
    "rather than failing. DISABLE_TELEMETRY=1 switches it off without a rebuild.",
    "",
    "⚠ The repotag below is a placeholder: this change REQUIRES a rebuild, because the exporter",
    "is compiled into the binary. The image running before it was",
    "  registry.gitlab.com/quip.network/faucet:qui1226-trial-2da7c89-amd64",
    "  @sha256:713f52cf22c13d63001da367f4d4d327f3ff209c80944cb4f57ad948a2bccc63",
    "Keep that here until the replacement is pinned — it is the rollback target.",
    "",
    "⭐ THE SUDO KEYS DIFFER PER CHAIN, and that is load-bearing. Measured 2026-09-02:",
    "  aglais       Sudo::Key = 0xc5c1e685...2510  (op-1 — this faucet's funder)",
    "  old testnet  Sudo::Key = 0xc6cb8a79...404b  (a DIFFERENT account)",
    "So ensure_funder_is_sudo(), which runs before the faucet serves anything, is an effective",
    "cross-chain guard: aimed at the wrong network this funder is not sudo there, the check fails,",
    "and the process refuses to start. It fails CLOSED. (An earlier revision of this comment",
    "claimed the funder was sudo on both chains and that nothing would catch a mis-aimed deploy;",
    "both halves were wrong.) ⚠ It is a coincidence of key choice, not a designed pin — if the two",
    "chains ever share a sudo key the guard silently evaporates.",
    "",
    "⚠ `--allow-any-chain` IS NOT A CHAIN PIN. It only disables verify_dev_chain(), which refuses",
    "any system_chain not starting with a dev prefix (chain.rs:405-413) — production has to set it.",
    "With it on there is no genesis or chain-identity check anywhere in the faucet, so the three",
    "--node-url values are the only thing aiming this app. The sudo-key guard above is what",
    "actually backstops a mistake here; check the URLs by eye anyway, and after deploy confirm the",
    "running faucet's target rather than assuming.",
    "",
    "BEFORE DEPLOYING:",
    "  1. repotag digest — build + push, then pin `repo:TAG@sha256:DIGEST`. A bare `repo@sha256:`",
    "     is REJECTED by Flux.",
    "  2. Enterprise ON — QUIP_FAUCET_FAUCET_KEY is the chain sudo key; empty `enterprise` ships",
    "     it as PLAINTEXT env. The toggle generates the blob at submit; it is not a pasted value.",
    "  3. Create the faucet.aglais.quip.network CNAME to the new app's FDM name once registered.",
    "     TLS is terminated by Flux FDM at the custom domain — there is no certifier/Tarsnap leg",
    "     here, unlike the bootnodes.",
    "  4. No environmentParameters value may be empty — Flux rejects the whole submission.",
    "     So omit ONEUPTIME_TELEMETRY_KEY entirely rather than leaving it blank.",
    "  5. This is an UPDATE to the existing quipfaucet app, not a new registration. Land the",
    "     repotag bump and the new env var in ONE submit: an env change needs a Soft Reinstall,",
    "     not a Restart, and at instances:1 each one is a real outage of the public faucet.",
    "",
    "REPLACE every <REPLACE:...> before use."
  ],
  "version": 8,
  "name": "quipfaucet",
  "description": "Quip aglais testnet faucet: native (SS58 / 0x+64-hex) and EVM (0x+40-hex) funding",
  "owner": "1H18UuAxugZihfoagL8xNT1Rpxn55dZtZ3",
  "compose": [
    {
      "name": "faucet",
      "description": "Rust quip-faucet. Funder is op-1's hybrid account (the chain sudo key); it sudo-mints a dedicated base wallet, funds the /sign pool, and serves /request, /sign, /health over plain HTTP. TLS is terminated by Flux FDM at the custom domain.",
      "repotag": "<REPLACE:registry.gitlab.com/quip.network/faucet:TAG-amd64@sha256:DIGEST — rebuild required, the OTLP exporter is compiled in. A bare repo@sha256: is REJECTED by Flux>",
      "ports": [
        8087
      ],
      "domains": [
        "faucet.aglais.quip.network"
      ],
      "environmentParameters": [
        "QUIP_FAUCET_FAUCET_KEY=<REPLACE:op-1 mnemonic from Proton Pass — same funder as the live faucet>",
        "RUST_LOG=info",
        "ONEUPTIME_TELEMETRY_KEY=<REPLACE:Telemetry Ingestion Key UUID from the OneUptime dashboard — project settings, Telemetry Ingestion Keys. Omit this line entirely to ship without telemetry; do NOT leave it blank>"
      ],
      "commands": [
        "--node-url=wss://bootnode-1.aglais.quip.network:20049/rpc",
        "--node-url=wss://bootnode-2.aglais.quip.network:20049/rpc",
        "--node-url=wss://bootnode-3.aglais.quip.network:20049/rpc",
        "--listen-host=0.0.0.0",
        "--port=8087",
        "--rate-limit-seconds=60",
        "--allow-any-chain"
      ],
      "containerPorts": [
        8087
      ],
      "containerData": "/data",
      "cpu": 1,
      "ram": 2048,
      "hdd": 1,
      "repoauth": "",
      "secrets": ""
    }
  ],
  "instances": 1,
  "contacts": [
    "F_S_CONTACTS=https://storage.runonflux.io/v1/contacts/892592545351050"
  ],
  "geolocation": [
    "acEU",
    "acNA",
    "a!cEU_RU"
  ],
  "nodes": [],
  "staticip": false,
  "enterprise": "<REPLACE:NOT a value you paste — flip the Enterprise toggle in the wizard's General tab, which generates the blob at submit. Leaving this off ships the chain sudo key as PLAINTEXT env>"
}
